# Resend Setup

1. Create API key and set `RESEND_API_KEY`.
2. Verify sending domain and set `EMAIL_FROM` (e.g. orders@mezcalomano.com).
3. Transactional emails are sent for order confirmation, shipping confirmation, refunds, and magic links.
